import React, {useEffect} from 'react'
import QrCode from 'qrcode.react'
import {QRBg} from './components'

const useQrBg = (qrText: string) => {
  useEffect(() => {}, [])
  return <QRBg />
}

export default useQrBg
